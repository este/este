machine:
  node:
    version: 6.9.1

# Disable cache because CircleCI doesn't detect deleted packages.
# circleci.com/docs/how-cache-works/#clearing-the-cache
dependencies:
  post:
    - rm -fr ~/.gradle
