enum EmailError {
  NO_TRAILING_SPACES
  REQUIRED
  EMAIL
  ALREADY_EXISTS
  NOT_EXISTS
}

enum PasswordError {
  NO_TRAILING_SPACES
  REQUIRED
  MIN_5_CHARS
  MAX_1024_CHARS
  WRONG_PASSWORD
}

enum Max140CharsError {
  NO_TRAILING_SPACES
  REQUIRED
  MAX_140_CHARS
}
