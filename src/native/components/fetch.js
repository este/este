import createFetch from '../../common/components/createFetch';
// TODO: Remove once react-native will support React 0.14
import React from 'react-native';

export default createFetch(React, React.Component);
