// /* @flow */
import React from 'react';
import {
  Box,
} from '../app/components';

const Container = (props) => (
  <Box {...props} />
);

const SwitchTheme = () => (
  <Container>
    {/* <Button>fok</Button> */}
  </Container>
);

export default SwitchTheme;
