import {create} from 'client/todos/actions';
import {expect} from 'chai';

describe('addTodo', () => {
  it('is a function', () => {
    // TODO: Add real test.
    expect(create).to.be.a('string');
  });
});
