import * as actions from 'client/todos/actions';
import {expect} from 'chai';

describe('addTodo', () => {
  it('is a function', () => {
    // TODO: Add real test.
    expect(actions.addTodo).to.be.a('function');
  });
});
