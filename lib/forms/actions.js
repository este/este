// @flow
import type { Action, FormsState } from '../../types';

// helper pro akce?
// jak na native?
// tam to proste treba nebude, ale jak bude vedet, co updatovat?

// TODO: Bude to moci bejt universalni?
// export const onUserChange = ()

// export const setField = (
//   id: string,
//   name: $Keys<Fields>,
//   value: any
// ): Action => ({
//   type: 'SET_FIELD',
//   payload: { id, name, value },
// });

// Maybe, but it must accept fields, and remove object if empty.
// export const resetFields = (id: string): Action => ({
//   type: 'RESET_FIELDS',
//   payload: { id },
// });
